fix SoC's dmem's ports (use _if?)
  Use adapter in toplevel module or SoC module (by using the dmem ports)
fix uart svh

asserations?
add a LSU to my core (Load/store unit)
fix utilization of Datawidth in core
fix core's repo top module's params